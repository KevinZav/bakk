## Example module just for reference.

### Usage
import module into `modules.module.ts` file and use it's services.

````ts
@Module({
  imports: [
    ...,
    ExampleModule
  ],
})
export class ModulesModule {}

````

